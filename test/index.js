var expect = require('chai').expect
var Api = require('../index')

describe('Api module', function () {
  it('initalises routes', function * () {
    var test = () => {
      return false
    }
    expect(test).to.eql(true)
  })
})
